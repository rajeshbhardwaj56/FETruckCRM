@model FETruckCRM.Models.GroupFormPermissionList
<style>

    * {
        list-style: none;
        margin: 0;
        padding: 0;
        font-size: 1em;
       /* cursor: pointer;*/
    }

    #menu {
        margin: 3px;
    }

        #menu > li { /* Top Level */
            float: left;
            margin-right: 3px;
            color:#000;
        }

            #menu > li > span {
                display: block;
                background: #6E8A9F;
                padding: 3px 10px;
            }

            #menu > li:hover > span {
                color: #fff;
            }

            #menu > li > ul { /* Second Level */
                display: none;
                background: #6E8A9F;
                
            }

            #menu > li:hover > ul {
                display: block;
                position: absolute;
                color:#000;
            }

            #menu > li > ul > li > span {
                display: block;
                padding: 3px 10px;
                border-top: solid 3px #fff;
            }

            #menu > li > ul > li:hover > span {
                color: #fff;
            }

            #menu > li > ul li > ul { /* Third Level & beyond */
                display: none;
                background: #0a112d;
            }

            #menu > li > ul li:hover > ul {
                display: block;
                position: absolute;
                left: 100%;
                border-left: solid 3px #fff;
                top: 0;
                width: auto;
            }

            #menu > li > ul > li ul > li {
                display: block;
                padding: 3px 3px;
                border-top: solid 3px #fff;
                white-space: nowrap;
            }

                #menu > li > ul > li ul > li:hover > span {
                    color: #fff;
                }
</style>
<div class="header__navbar m-t-30 m-b-30">
    <button class="hamburger hidedesktop" type="button">
        <span class="hamburger-box">
            <span class="hamburger-inner"></span>
        </span>
    </button>
    <ul id="menu" class="list-unstyled">
        @*@if (Model.FormPermissionList.Where(x => x.FormID == 1 && x.IsFormPermissions == true && x.FormLevel == 1).Count() > 0 || Model.FormPermissionList.Where(x => x.ParentID == 1 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
            {
                <li>
                    <a href="@Url.Action("Index", "Dashboard")">
                        <span class="bot-line"></span>Menu
                    </a>
                    <ul>*@
        @if (Model.FormPermissionList.Where(x => (x.FormID == 2 || x.FormID == 11 || x.FormID == 15 || x.FormID == 14 || x.FormID == 16 || x.FormID == 13) && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
        {
            <li class="has-sub">
                <a href="#">
                    <span class="bot-line"></span>Administrator
                </a>

                <ul class="header3-sub-list list-unstyled">
                    @if (Model.FormPermissionList.Where(x => x.FormID == 2 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("FormPermission", "User")">Form Permissions</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 11 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "User")">User Creation</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 15 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "EquipmentType")">Equipment Type</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 14 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "FactoringCompany")">Factoring Company</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 16 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("AddPreference", "Preference")">T & C Notes</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 13 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "CustomBroker")">Custom Broker</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 13 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Email")">Email Alerts</a>
                        </li>
                    }
                    @if (Session["RoleID"].ToString() == "1")
                    {
                        if (Model.FormPermissionList.Where(x => x.FormID == 13 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                        {
                            <li>
                                <a href="@Url.Action("Index", "Log")">Log</a>
                            </li>
                        }
                    }

                </ul>
            </li>
        }

        @if (Model.FormPermissionList.Where(x => (x.FormID == 9 || x.FormID == 10) && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
        {
            <li class="has-sub">
                <a href="#">
                    <span class="bot-line"></span>Compliance
                </a>
                <ul class="header3-sub-list list-unstyled">

                    @if (Model.FormPermissionList.Where(x => x.FormID == 9 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("ASIndex", "Customer")">Pending Customer</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 10 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("ASIndex", "Career")">Pending External Carrier</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 20 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("PendingLoads", "Load")">Pending Loads</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 20 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("ViewMCCheckIndex", "Career")">Pending MC Check</a>
                        </li>
                    }
                </ul>

            </li>
        }
        @if (Model.FormPermissionList.Where(x => (x.FormID == 3 || x.FormID == 4 || x.FormID == 5 || x.FormID == 8 || x.FormID == 12) && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
        {
            <li class="has-sub">
                <a href="#">
                    <span class="bot-line"></span>Dispatch
                </a>
                <ul class="header3-sub-list list-unstyled">

                    @if (Model.FormPermissionList.Where(x => x.FormID == 3 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Customer")">Customer</a>
                        </li>
                    }
                    @if (Session["RoleID"].ToString() == "1")
                    {
                        if (Model.FormPermissionList.Where(x => x.FormID == 3 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                        {
                            <li>
                                <a href="@Url.Action("AssignCustomer", "User")">Assign Customer</a>
                            </li>
                        }
                    }

                    @if (Model.FormPermissionList.Where(x => x.FormID == 4 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Shipper", new { type = 1 })">Shipper</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 5 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Shipper", new { type = 2 })">Consignee</a>
                        </li>
                    }

                    @if (Model.FormPermissionList.Where(x => x.FormID == 8 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Career")">External Carrier</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 12 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li>
                            <a href="@Url.Action("Index", "Dashboard")">Load Creation</a>
                        </li>
                    }
                    @if (Model.FormPermissionList.Where(x => x.FormID == 12 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        <li style="display:block;">
                            <a href="@Url.Action("MCCheckIndex", "Career")">MC Check</a>
                        </li>
                    }
                    @if (Convert.ToInt64(Session["UserID"]) == 7 || Convert.ToInt64(Session["UserID"]) == 131)
                    {
                        <li>
                            <a href="@Url.Action("ManagerReports", "Load")">Raw Report</a>
                        </li>
                    }
                </ul>
            </li>
        }

        @if (Convert.ToInt64(Session["UserID"]) == 1 || Convert.ToInt64(Session["UserID"]) == 16 )
        {

            if (Model.FormPermissionList.Where(x => (x.FormID == 18) && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
            {
            <li class="has-sub">
                <a href="#">
                    <span class="bot-line"></span>Reporting
                </a>
                <ul class="header3-sub-list list-unstyled">

                    @if (Model.FormPermissionList.Where(x => x.FormID == 12 && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
                    {
                        @*<li>
                            <a href="@Url.Action("RawReport", "Load")">Load Report</a>
                        </li>*@
                        <li>
                            <a href="@Url.Action("ViewRawReport", "Load")">Load Report</a>
                        </li>
                    }

                </ul>
            </li>
        }
        if (Model.FormPermissionList.Where(x => (x.FormID == 17) && x.IsFormPermissions == true && x.FormLevel == 2).Count() > 0)
        {
            <li class="has-sub">
                <a href="#">
                    <span class="bot-line"></span>Accounting
                </a>
                <ul class="header3-sub-list list-unstyled">
                    <li>
                        <a href="@Url.Action("PendingLoads", "Load")">Pending Loads</a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index", "Dashboard")">Load List</a>
                    </li>
                    <li>
                        <a href="@Url.Action("CustomerMasterReport", "Customer")">Customer Master Report</a>
                    </li>
                    <li>
                        <a href="@Url.Action("CarrierMasaterReport", "Career")">Carrier Master Report</a>
                    </li>
                    <li>
                        <a href="@Url.Action("AccReceReport", "Load")">Account Recievable Report</a>
                    </li>

                    <li>
                        <a href="@Url.Action("AccPayReport", "Load")">Account Payable Report</a>
                    </li>

                </ul>
            </li>
            }
        }
        @*</ul>




                </li>

            }*@
        @*<li>
                <a href="#">
                    <span class="bot-line"></span>IFTA
                </a>
            </li>
            <li>
                <a href="#">
                    <span class="bot-line"></span>Sales Manager
                </a>
            </li>
            <li>
                <a href="#">
                    <span class="bot-line"></span>Accounting
                </a>
            </li>
            <li>
                <a href="#">
                    <span class="bot-line"></span>Reports
                </a>
            </li>
            <li>
                <a href="#">
                    <span class="bot-line"></span>Add-Ons
                </a>
            </li>
            <li>
                <a href="#">
                    <span class="bot-line"></span>abc.intigration
                </a>
            </li>
            <li>
                <a href="#">
                    <span class="bot-line"></span>Help
                </a>
            </li>*@
        @*<li class="el_head_select_li">
                <select class="form-control-sm el_head_select form-control">
                    <option value="0">Display - Shipper Rate</option>
                    <option value="1">Shipper Rate</option>
                    <option value="2">Carrier Pay</option>
                    <option value="3">Load Margin</option>
                    <option value="4">Margin %</option>
                </select>
            </li>*@
    </ul>
</div>